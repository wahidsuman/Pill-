package com.pillreminder.app.di

import org.koin.dsl.module

val appModule = module {
    // TODO: Add dependency injection modules here
    // Example:
    // single<Repository> { RepositoryImpl() }
    // factory<UseCase> { UseCaseImpl(get()) }
}